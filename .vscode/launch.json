{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch Cylinder Unwrapper",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/unwrap_app.exe", // <-- Đảm bảo tên file exe đúng
            "args": [], // Chương trình này không cần tham số
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}", // Chạy từ thư mục gốc của project
            "environment": [],
            "console": "externalTerminal"
        }
    ]
}